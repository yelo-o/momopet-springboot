server:
  port: 9090

spring:
#  datasource:
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create-drop # application 실행 시점에 가지고 있는 Entity 정보를 보고 테이블을 모두 지우고 재생
#    properties:
#      hibernate:
##        show_sql: true # log에 출력되는 것이 아니라 sysout에 출력 됨
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace
