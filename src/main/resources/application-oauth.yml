spring:
  security:
    oauth2:
      client:
        registration:
          # 구글 설정
          google:
            client-id: 877563063519-tot5n5iruif61c0rhbfbmkbhb8hb39vv.apps.googleusercontent.com
            client-secret: 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
            scope:
              - profile
              - email
          # 네이버 설정
          naver:
            client-id: smQMGJRAMZW6pmjHgE_Q
            client-secret: YDdRIqwFAB
            scope:
              - profile
              - email
            client-name: Naver
            authorization-grant-type: authorization_code # oauth2의 동작 방식은 여러가지가 있는데, 코드를 응답받는 방식으로 진행한다는 의미
            redirect-uri: "{baseUrl}/{action}/oauth2/code/{registrationId}"

        # 네이버의 경우, 따로 provider 설정을 추가해줘야 한다.
        provider:
          # 아래의 링크들은 네이버 로그인 개발 가이드에 있다.
          naver:
            authorization-uri: https://nid.naver.com/oauth2.0/authorize
            token-uri: https://nid.naver.com/oauth2.0/token
            user-info-uri: https://openapi.naver.com/v1/nid/me
            user-name-attribute: response